<div class="row">
    {{#if burgers}}
    <div class="col-md-6">
        <ul>
            {{#each burgers}}
            {{#unless burgers.eaten_status}}
            <li>
                <p>{{this.id}}: {{this.burger_name}}</p>
                <button type="submit" data="{{this.id}}">Eat it!</button>
            </li>
            {{/unless}}
            {{/each}}
        </ul>
    </div>


    {{!-- //////////////////////////////////////////////////////////////////////////////////// --}}

    <div class="col-md-6">
        <ul>
            {{#each burgers}}
            {{#if burgers.eaten_status}}
            <li>
                <p>{{this.id}}: {{this.burger_name}}</p>
            </li>
            {{/if}}
            {{/each}}
        </ul>
    </div>
</div>

{{/if}}
